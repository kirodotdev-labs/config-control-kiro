/**
 * @fileoverview Feature flags for toggling application capabilities.
 */
/** @type {{MCP: boolean, AGENTS: boolean}} Feature flags for enabling/disabling features */
export const FEATURES = {
  MCP: true,
  AGENTS: true,
};
